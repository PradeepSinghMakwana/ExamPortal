using System;
using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace ExamPortal.Tests.Models
{
    /*
    [TestClass]
    public class ValidateTest
    {
        
        [TestMethod]
        public void Validate_StudentPreviousQualification_Given_Valid_StudentPreviousQualification_ExpectNoValidationErrors()
        {
            // Arrange
            var model = TestModelHelper.MockStudentPreviousQualification();
            //model.faculty_name = "Vogel";

            // Act
            var results = TestModelHelper.Validate(model);

            // Assert
            Assert.AreEqual(0, results.Count);
        }

        [TestMethod]
        public void Validate_StudentCurrentQualification_Given_Valid_StudentCurrentQualification_ExpectNoValidationErrors()
        {
            // Arrange
            var model = TestModelHelper.MockStudentCurrentQualification();
            //model.faculty_name = "Vogel";

            // Act
            var results = TestModelHelper.Validate(model);

            // Assert
            Assert.AreEqual(0, results.Count);
        }
    
        [TestMethod]
        public void Validate_RegisteredCandidates_Given_Valid_RegisteredCandidates_ExpectNoValidationErrors()
        {
            // Arrange
            var model = TestModelHelper.MockRegisteredCandidates();
            //model.faculty_name = "Vogel";

            // Act
            var results = TestModelHelper.Validate(model);

            // Assert
            Assert.AreEqual(0, results.Count);
        }

        [TestMethod]
        public void Validate_Loger_Given_Valid_Loger_ExpectNoValidationErrors()
        {
            // Arrange
            var model = TestModelHelper.MockLoger();
            //model.faculty_name = "Vogel";

            // Act
            var results = TestModelHelper.Validate(model);

            // Assert
            Assert.AreEqual(0, results.Count);
        }

        [TestMethod]
        public void Validate_AssignedTest_Given_Valid_AssignedTest_ExpectNoValidationErrors()
        {
            // Arrange
            var model = TestModelHelper.MockAssignedTest();
            //model.faculty_name = "Vogel";

            // Act
            var results = TestModelHelper.Validate(model);

            // Assert
            Assert.AreEqual(0, results.Count);
        }

        [TestMethod]
        public void Validate_Admin_Given_Valid_Admin_ExpectNoValidationErrors()
        {
            // Arrange
            var model = TestModelHelper.MockAdmin();
            //model.faculty_name = "Vogel";

            // Act
            var results = TestModelHelper.Validate(model);

            // Assert
            Assert.AreEqual(0, results.Count);
        }

        [TestMethod]
        public void Validate_Question_Given_Valid_Question_ExpectNoValidationErrors()
        {
            // Arrange
            var model = TestModelHelper.MockQuestion();
            //model.faculty_name = "Vogel";

            // Act
            var results = TestModelHelper.Validate(model);

            // Assert
            Assert.AreEqual(0, results.Count);
        }

        [TestMethod]
        public void Validate_TestQuestion_Given_Valid_TestQuestion_ExpectNoValidationErrors()
        {
            // Arrange
            var model = TestModelHelper.MockTestQuestion();
            //model.faculty_name = "Vogel";

            // Act
            var results = TestModelHelper.Validate(model);

            // Assert
            Assert.AreEqual(0, results.Count);
        }

        [TestMethod]
        public void Validate_Teaches_Given_Valid_Teaches_ExpectNoValidationErrors()
        {
            // Arrange
            var model = TestModelHelper.MockTeaches();
            //model.faculty_name = "Vogel";

            // Act
            var results = TestModelHelper.Validate(model);

            // Assert
            Assert.AreEqual(0, results.Count);
        }

        [TestMethod]
        public void Validate_Test_Given_Valid_Test_ExpectNoValidationErrors()
        {
            // Arrange
            var model = TestModelHelper.MockTest();
            //model.faculty_name = "Vogel";

            // Act
            var results = TestModelHelper.Validate(model);

            // Assert
            Assert.AreEqual(0, results.Count);
        }

        [TestMethod]
        public void Validate_Class_Given_Valid_Class_ExpectNoValidationErrors()
        {
            // Arrange
            var model = TestModelHelper.MockClass();
            //model.faculty_name = "Vogel";

            // Act
            var results = TestModelHelper.Validate(model);

            // Assert
            Assert.AreEqual(0, results.Count);
        }

        [TestMethod]
        public void Validate_Teacher_Given_Valid_Teacher_ExpectNoValidationErrors()
        {
            // Arrange
            var model = TestModelHelper.MockTeacher();
            //model.faculty_name = "Vogel";

            // Act
            var results = TestModelHelper.Validate(model);

            // Assert
            Assert.AreEqual(0, results.Count);
        }

        [TestMethod]
        public void Validate_Student_Given_Valid_Student_ExpectNoValidationErrors()
        {
            // Arrange
            var model = TestModelHelper.MockStudent();
            //model.unique_name = "Vogel";

            // Act
            var results = TestModelHelper.Validate(model);

            // Assert
            Assert.AreEqual(0, results.Count);
        }

        [TestMethod]
        public void Validate_Course_Given_Valid_Course_ExpectNoValidationErrors()
        {
            // Arrange
            var model = TestModelHelper.MockCourse();

            // Act
            var results = TestModelHelper.Validate(model);

            // Assert
            Assert.AreEqual(0, results.Count);
        }

        [TestMethod]
        public void Validate_Subject_Given_Valid_Subject_ExpectNoValidationErrors()
        {
            // Arrange
            var model = TestModelHelper.MockSubject();

            // Act
            var results = TestModelHelper.Validate(model);

            // Assert
            Assert.AreEqual(0, results.Count);
        }

    }
    */
}
